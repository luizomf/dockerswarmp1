# shellcheck disable=SC2034
# Essa variável controla se você está em ambiente de produção ou
# desenvolvimento. Só usamos dois valores:
#
# development = seu computador local, testes e build
# production = servidor real em produção
CURRENT_ENV=development

# Este e-mail é para o certbot/letsencrypt.
# É recomendável usar um bom e-mail para receber notificações importantes.
EMAIL="seu_email@email.com"

# Domínio usado pelo Traefik (frontend e API).
APP_DOMAIN="app.myswarm.cloud"

# Configuração do PostgreSQL (usado pela API).
POSTGRES_DB="dockerswarmp1"
POSTGRES_USER="dockerswarmp1"

# ISSO NÃO É USADO EM PROD
# VER -> SECRETS
# Gere um valor com: python3 -c "import secrets; print(secrets.token_hex(32))"
POSTGRES_PASSWORD="ANY_VALUE"

# Opcional, mas recomendado: sal fixo para anonimizar visitantes.
# Se ficar vazio, a API usa GITHUB_WEBHOOK_SECRET como fallback.
# Trocar esse valor (ou o webhook secret) muda o hash dos visitantes e pode
# "resetar" a contagem de visitas do dia.
# Gere um valor com: python3 -c "import secrets; print(secrets.token_hex(32))"
VISIT_SALT="ANY_VALUE"

# Explico como gerar este secreve no DEV_GUIDE.md
# Gere um valor com: python3 -c "import secrets; print(secrets.token_hex(32))"
GITHUB_WEBHOOK_SECRET='ANY_VALUE'

# Branch principal do seu repositório
DEPLOY_BRANCH="main"

# ID de usuário e grupo para a aplicação. Não precisa mexer.
APP_UID=1011
APP_GID=1011

# vim:sw=2:ts=2:et:set filetype=bash :
